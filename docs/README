This is SYMEESS documentation written using sphinx

Sphinx should be installed in your system. Use pip to install it.

   pip install sphinx --user



Use provided Makefile to compile the documentation:

HTML output:

   make html

Latex document output (requires latex to be installed in your system):

   make latexpdf

